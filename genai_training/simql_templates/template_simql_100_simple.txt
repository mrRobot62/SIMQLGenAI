mmp {mmp_alias} 
 
    register "{mmp_name}"
     
    section measurements: 
        vMP1 reference "{mp1}" 
        vMP1 reference "{mp2}" 
 
    section variables: 
        define vTmp1, vTmp2, vResult 
         
    section logic: #all the logic goes here

        vTmp1 = LOAD DATA(ref=[vMP1], {refdate}, range={range})
        vTmp2 = LOAD DATA(ref=[vMP2], {refdate}, range={range})
        vResult = COMPUTE DATA(variables=[vTmp1, vTmp2], math={math})
    
    section result:
        save  vResult #save our final result
     
end 